Given("the following comments exist") do |comment|
    table.hashes.each do |comment|
  end
end

  #Given("the following user exist in the database") do |table|
  #  table.hashes.each do |user|
  #      User.create(user)
  #  end 

  Then("I should be able to leave a {string} under each published article") do |string|
    pending # Write code here that turns the phrase above into concrete actions
  end
  
  Then("I can optionally provide {string} that will be displayed together with the comment") do |string|
    pending # Write code here that turns the phrase above into concrete actions
  end
  
  